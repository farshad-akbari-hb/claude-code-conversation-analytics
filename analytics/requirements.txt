# Claude Analytics Platform Dependencies
# Install with: pip install -r requirements.txt

# MongoDB extraction
pymongo>=4.6

# Data processing
pyarrow>=15.0
pandas>=2.0

# Apache Iceberg
pyiceberg[duckdb,pyarrow]>=0.8.0

# DuckDB OLAP database
duckdb>=0.10

# Orchestration (Prefect) - pinned to match server version
prefect==2.19.9

# Data quality (Great Expectations)
great-expectations>=0.18

# Configuration management
python-dotenv>=1.0
pydantic>=2.0,<2.10
pydantic-settings>=2.0,<2.7

# CLI interface
typer>=0.9
rich>=13.0

# dbt for transformations
dbt-core>=1.7
dbt-duckdb>=1.7

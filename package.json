{
  "name": "claude-mongo-sync-workspace",
  "private": true,
  "description": "Claude Code conversation sync and analytics platform",
  "scripts": {
    "dev": "npm run dev --prefix sync-service",
    "build": "npm run build --prefix sync-service",
    "start": "npm run start --prefix sync-service",
    "lint": "npm run lint --prefix sync-service",
    "dev:ui": "npm run dev --prefix ui",
    "build:ui": "npm run build --prefix ui",
    "start:ui": "npm run start --prefix ui"
  }
}
